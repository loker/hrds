{%
  set classes = [
    'py-3',
    'mb-4',
    'view-id-' ~ id,
    'view-display-id-' ~ display_id,
  ]
%}
<div{{ attributes.addClass(classes) }}>
<div>{{ casteski }}</div>
<div class="row row-cols-1 row-cols-md-2 g-4 my-3 container">
  {% if rows %}
      {{ rows }}
  {% elseif empty %}
    <div class="view-empty">
      {{ empty }}
    </div>
  {% endif %}
</div>
</div>
