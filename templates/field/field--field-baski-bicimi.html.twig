    <h5><strong>{{ baskilar }}:</strong>
    {% for item in items %}
      {% if loop.last %}
        {% set separator = '' %}
      {% else %}
        {% set separator = ' | ' %}
      {% endif %}
      {{ item.content }}{{ separator }}
    {% endfor %}
    </h5>
